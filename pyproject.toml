[project]
name = "logit-tuned-lens"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "torch",
    "transformer-lens",
    "fastapi",
    "uvicorn",
    "datasets",
    "pandas",
    "matplotlib",
    "seaborn",
]
